# Added only when the protected AGENTS.md/orchestrator context is active.
services:
  hermes:
    volumes:
      - type: bind
        source: ${HERMES_HOST_ROOT}/AGENTS.md
        target: /policy-context/AGENTS.md
        read_only: true
